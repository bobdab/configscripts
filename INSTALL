These are my personal install scripts for VMs and desktops.

The shortcut to install is to use the short url to download the thing
that will download the full package:

http://tinyurl.com/m4q74zt
You can manually download from:

https://github.com/bobdab/configscripts/archive/master.tar.gz

unzip, untar, look for the setup scripts under the FreeBSD directory.
